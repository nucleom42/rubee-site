- name: Deploy to Raspberry Pi
  uses: appleboy/ssh-action@v1.0.3
  with:
    host: ${{ secrets.HOST }}
    username: ${{ secrets.USER }}
    key: ${{ secrets.SSH_KEY }}
    port: 22
    script: |
      cd ~/rubee
      git pull origin main
      chmod +x ./deploy.sh
      ./deploy.sh
